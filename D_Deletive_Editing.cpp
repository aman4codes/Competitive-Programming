#include <bits/stdc++.h>
using namespace std;

int main()
{
    int t;
    cin >> t;
    while (t--)
    {
        string s, t;
        cin >> s >> t;
        string check;
        for (int i = 0; i < t.length(); i++)
        {
            for (int k = 0; k < s.length(); k++)
            {
                if (s[k] == t[i])
                {

                }
            }
        }
    }
}